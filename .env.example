# VERITY — root environment (Phase 0).
#
# The Cairo toolchain (Scarb 2.20.1, Starknet Foundry 0.63.0) runs inside WSL
# Ubuntu-24.04. See scripts/setup-wsl-toolchain.sh and docs/STRK20_INTEGRATION.md.
#
# Frontend configuration lives in apps/web/.env.example.
# Mainnet secrets/keys are NEVER committed; all values here are placeholders.